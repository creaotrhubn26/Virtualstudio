node_modules/
dist/
.vite/
*.log
.DS_Store

# ML Modeller og store binærfiler
backend/models/
# backend/rodin_models/ - Now tracked in git
# backend/humaniflow/model_files/ - Tracked with Git LFS
backend/downloads/
backend/test_images/
backend/outputs/
# Locally-cached GLBs from Meshy + BlenderKit resolvers — source of truth
# is R2; these directories are pure disk-caches and can grow huge.
backend/blenderkit_models/
backend/meshy_models/

# Vendored upstream ML repos — these should be pip-installed, not checked in.
# See backend/VENDORING.md for the migration plan. New clones will skip them
# once they are removed from the tree. Existing tracked entries stay until
# explicitly `git rm`-ed.
backend/detectron2_install/
backend/facexformer_repo/
backend/humaniflow/
backend/sam3d_repo/
backend/trellis_models/
backend/trellis_uploads/

# Accidental shell-redirect files from `pip install X >=0.20.0`
backend/=*

# Store assets
# attached_assets/ - Now tracked in git
*.pth
*.tar
*.tar.gz
*.zip
*.7z

# Cache
.cache/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
# backend/.wrangler/ is tracked with Git LFS

# Build artifacts
build/
.next/
*.egg-info/
*.so
*.dylib

# Database
*.db
*.sqlite
*.sqlite3

# Environment
.env
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db
.DS_Store

# Claude Code local state
.claude/

# E2E probe output (screenshots + assembly dumps from scripts/*_probe.mjs)
tmp-e2e-probe/

# Large binary asset packs — too big for git, keep alongside the repo
contexts/*.pak
