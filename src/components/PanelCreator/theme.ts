/**
 * Theme configuration for PanelCreator
 */

import { createTheme } from '@mui/material';

export const panelCreatorTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#00d4ff',
    },
    secondary: {
      main: '#58a6ff',
    },
    background: {
      default: '#0d1117',
      paper: '#1c2128',
    },
  },
});














